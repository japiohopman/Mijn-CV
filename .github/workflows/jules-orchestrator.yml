name: Jules Queue Orchestrator v2

# Issue-Driven Jules Orchestrator workflow with scheduled recovery heartbeat.
# Runs on push to orchestrator files, scheduled cron, or manual dispatch.

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/jules-orchestrator.yml'
      - 'scripts/jules-orchestrator.mjs'
      - 'JULES_ORCHESTRATOR_V2.md'
  schedule:
    - cron: '7,22,37,52 * * * *'
  workflow_dispatch: {}

permissions:
  contents: write
  pull-requests: read
  issues: write

concurrency:
  group: jules-orchestrator
  cancel-in-progress: false

jobs:
  orchestrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: ${{ github.event.repository.default_branch }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run orchestrator v2
        env:
          JULES_API_KEY: ${{ secrets.JULES_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          JULES_SOURCE: sources/github/${{ github.repository }}
        run: node scripts/jules-orchestrator.mjs
